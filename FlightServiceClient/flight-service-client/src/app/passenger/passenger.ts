export interface Passenger {
    id: number;
    name: string;
    age: string;
    email: string;
    occupation: string;
}

export interface PassengerDTO {
    name: string;
    age: string;
    email: string;
    occupation: string;
}